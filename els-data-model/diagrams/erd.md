# Entity-Relationship Diagram

Auto-generated by scripts/generate-diagram/generate_diagram.py. Do not edit by hand.
- Source: els-data-model/model/entities/*.json
- Regenerate with: python generate_diagram.py

Rendered with [Mermaid](https://mermaid.js.org/), which GitHub renders natively inside Markdown — no extra tooling needed to view it.

```mermaid
erDiagram
    CAMPUS {
        integer id PK
        string uuid "External identification"
        string code "Human-memorable code"
        string name
        text description
    }

    COURSE {
        integer campus_id PK,  FK
        integer id PK
        string course_type FK
        string name
        string course_number "External identification"
        text description
    }

    COURSE_CONTENT {
        integer campus_id PK,  FK
        integer id PK
        integer course_id FK
        string content_type FK
        integer ordering_position
        boolean active
        string document_text "Nullable"
    }

    COURSE_CONTENT_TYPE {
        string code PK
        string display_name
        integer sort_order
        boolean is_active
    }

    COURSE_PERSON {
        integer campus_id PK,  FK
        integer id PK
        integer course_id FK
        integer person_id FK
        string course_role FK
    }

    COURSE_PERSON_ROLE {
        string code PK
        string display_name
        integer sort_order
        boolean is_active
    }

    COURSE_TEST {
        integer campus_id PK,  FK
        integer id PK
        integer course_id FK
        string code
        boolean active
        string test_questions "Nullable"
        integer possible_score
        integer required_score
    }

    COURSE_TYPE {
        string code PK
        string display_name
        integer sort_order
        boolean is_active
    }

    CURRICULUM_TYPE {
        string code PK
        string display_name
        integer sort_order
        boolean is_active
    }

    PERSON {
        integer campus_id PK,  FK
        integer id PK
        string uuid "External identification"
        string first_name
        string last_name
    }

    SEMESTER_TYPE {
        string code PK
        string display_name
        integer sort_order
        boolean is_active
    }

    STUDY_PROGRAM {
        integer campus_id PK,  FK
        integer id PK
        string code "External identification"
        string name
        text description
    }

    SUBMISSION {
        integer campus_id PK,  FK
        integer id PK
        integer course_person_id FK
        integer course_test_id FK
        datetime submitted_time
        string submission_text "Nullable"
        integer attempt_number
        integer score
    }

    TERM {
        integer campus_id PK,  FK
        integer id PK
        integer study_program_id FK
        string semester_type FK
    }

    TERM_COURSE {
        integer campus_id PK,  FK
        integer id PK
        integer term_id FK
        integer course_id FK
        string curriculum_type FK
    }


    CAMPUS ||--o{ COURSE : "offers"
    COURSE_TYPE ||--o{ COURSE : "categorizes"
    CAMPUS ||--o{ COURSE_CONTENT : "encompases"
    COURSE ||--o{ COURSE_CONTENT : "includes"
    COURSE_CONTENT_TYPE ||--o{ COURSE_CONTENT : "categorizes"
    CAMPUS ||--o{ COURSE_PERSON : "encompases"
    COURSE ||--o{ COURSE_PERSON : "is involved in"
    PERSON ||--o{ COURSE_PERSON : "is involved in"
    COURSE_PERSON_ROLE ||--o{ COURSE_PERSON : "categorizes"
    CAMPUS ||--o{ COURSE_TEST : "encompases"
    COURSE ||--o{ COURSE_TEST : "includes"
    CAMPUS ||--o{ PERSON : "is attended by"
    CAMPUS ||--o{ STUDY_PROGRAM : "offers"
    CAMPUS ||--o{ SUBMISSION : "encompases"
    COURSE_PERSON ||--o{ SUBMISSION : "submits"
    COURSE_TEST ||--o{ SUBMISSION : "has submission"
    CAMPUS ||--o{ TERM : "schedules"
    STUDY_PROGRAM ||--o{ TERM : "schedules"
    SEMESTER_TYPE ||--o{ TERM : "categorizes"
    CAMPUS ||--o{ TERM_COURSE : "consists of"
    COURSE ||--o{ TERM_COURSE : "is scheduled in"
    TERM ||--o{ TERM_COURSE : "consists of"
    CURRICULUM_TYPE ||--o{ TERM_COURSE : "categorizes"
```

## Why an ER diagram instead of a UML class diagram

Mermaid supports both `classDiagram` (UML class notation — attributes, methods, associations) and `erDiagram` (entity-relationship notation — tables, keys, cardinality). `erDiagram` was used here because it maps directly onto relational concepts like composite primary keys and foreign keys, which is what this model actually is. If a later component models these entities as application-level classes (an ORM layer, a domain model in C# or Python), a `classDiagram` view showing that object model would be a reasonable and complementary addition at that point — the two notations answer different questions.
