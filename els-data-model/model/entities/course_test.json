{
  "entity": "CourseTest",
  "table": "course_test",
  "comment": "A test (quiz/exam) belonging to a course, with a possible and required score.",
  "columns": [
    {
      "name": "campus_id",
      "type": "integer",
      "nullable": false,
      "primaryKey": true,
      "foreignKeys": [
        { "name": "Campus", "entity": "Campus", "column": "id", "relation": "encompases" },
        { "name": "Course", "entity": "Course", "column": "campus_id", "relation": "includes" }
      ],
      "comment": "Campus this course test belongs to."
    },
    {
      "name": "id",
      "type": "integer",
      "nullable": false,
      "primaryKey": true,
      "identity": true,
      "comment": "Surrogate identifier for the course test, auto-generated by the database and unique across the whole system."
    },
    {
      "name": "course_id",
      "type": "integer",
      "nullable": false,
      "foreignKeys": [
        { "name": "Course", "entity": "Course", "column": "id", "relation": "includes" }
      ],
      "comment": "Course this test belongs to."
    },
    {
      "name": "code",
      "type": "string",
      "length": 50,
      "nullable": false,
      "uniqueWith": ["course_id"],
      "comment": "Human-readable test code, unique within its course (the same code may be reused on a different course)."
    },
    {
      "name": "active",
      "type": "boolean",
      "nullable": false,
      "comment": "Whether this test is currently open/available."
    },
    {
      "name": "test_questions",
      "type": "string",
      "length": 4000,
      "nullable": true,
      "comment": "Optional test question text, capped at 4000 characters (SQL Server's largest non-MAX NVARCHAR size) rather than using the unbounded text type.",
      "shortDescription": "Nullable"
    },
    {
      "name": "possible_score",
      "type": "integer",
      "nullable": false,
      "comment": "Maximum score achievable on this test."
    },
    {
      "name": "required_score",
      "type": "integer",
      "nullable": false,
      "comment": "Minimum score required to pass this test."
    }
  ]
}
