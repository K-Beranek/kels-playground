{
  "entity": "Submission",
  "table": "submission",
  "comment": "A person's attempt at a course test.",
  "columns": [
    {
      "name": "campus_id",
      "type": "integer",
      "nullable": false,
      "foreignKey": { "entity": "Campus", "column": "id" },
      "comment": "Campus this submission belongs to."
    },
    {
      "name": "course_person_id",
      "type": "integer",
      "nullable": false,
      "foreignKey": { "entity": "CoursePerson", "column": "id" },
      "comment": "The course-person (person + course + role) this submission was made by."
    },
    {
      "name": "course_test_id",
      "type": "integer",
      "nullable": false,
      "foreignKey": { "entity": "CourseTest", "column": "id" },
      "comment": "The test this submission is an attempt at. The course referenced by course_person_id and the course referenced by course_test_id must be the same course; that rule is not enforced at the database level, only expected of the data."
    },
    {
      "name": "id",
      "type": "integer",
      "nullable": false,
      "primaryKey": true,
      "identity": true,
      "comment": "Surrogate identifier for the submission, auto-generated by the database and unique across the whole system."
    },
    {
      "name": "submission_text",
      "type": "string",
      "length": 4000,
      "nullable": true,
      "comment": "Optional submitted answer text, capped at 4000 characters (SQL Server's largest non-MAX NVARCHAR size) rather than using the unbounded text type."
    },
    {
      "name": "attempt_number",
      "type": "integer",
      "nullable": false,
      "uniqueWith": ["campus_id", "course_person_id", "course_test_id"],
      "comment": "Which attempt this is for this course-person on this test (1st, 2nd, ...). Unique together with campus_id, course_person_id, and course_test_id: the same attempt number can't be recorded twice for the same course-person/test."
    },
    {
      "name": "score",
      "type": "integer",
      "nullable": false,
      "comment": "Score achieved on this attempt."
    }
  ]
}
